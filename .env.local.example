# ============================================================
# Salin file ini menjadi .env.local lalu isi nilainya.
# JANGAN commit .env.local (sudah diabaikan git).
# ============================================================

# Supabase — dari Project Settings → API di dashboard Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...        # anon public key (aman di browser)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...            # service_role key — RAHASIA, server only

# Base URL situs (untuk membentuk link undangan)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Payment aggregator (sandbox) — diisi nanti saat integrasi template & amplop
# XENDIT_SECRET_KEY=xnd_development_...
